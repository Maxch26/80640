const formulario = document.getElementById("formulario")
const a1Fieldset = (legend, txt1, txt2) =>{
    return
    <fieldset>
        <legend>${legend}</legend>
        <label for = CSS={(txt1)}>(txt2)</label>
    </fieldset>
}